#include <bits/stdc++.h>
using namespace std;

int main() {
    int i = 1; // Set i to 1 to ensure the loop runs at least once
    do {
        cout << "yash" << endl;
        i++;
    } while (i <= 2); // Change the condition to i <= 2 for two iterations

    return 0;
}
