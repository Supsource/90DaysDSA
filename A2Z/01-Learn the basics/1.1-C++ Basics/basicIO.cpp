#include <iostream>  // For standard input and output
#include <cmath>     // For math functions
#include <string>    // For string handling

using namespace std; // To avoid writing std:: before standard library elements

int main() {
    // Uncommented lines for standard output
    // cout << "hey yash" << "\n";
    // cout << "hey boss" << endl << "hey raj" << "\n";

    // Declaring variables for input
    int x, y;

    // Prompting the user for input
    cout << "Enter values for x and y: ";
    
    // Input from the user
    cin >> x >> y;

    // Output the values of x and y
    cout << "Value of x: " << x << " and y: " << y << endl;

    return 0;
}
